sebelum melakukan instal, ikuti langkah berikut :
    
    1. sudo su (pastikan sudah masuk kedalam mode root)
    2. apt update
    3. cd Downloads (masuk kedalam cd downloads)
    4. ls (untuk melihat isi dalam folder downloads)
    5. dpkg -i (nama apliksi, atau bisa copy paste dari nama file yang ada di folder downloads)
    6. apt-get -f install (lakukan ini jika mengalami error dalam dpkg, jika tidak berarti sudah terinstall)
    
    jika sudah melakukan instalasi dan tidak terjadi error, siahkan lanjutkan ke step berikutnya yaitu extension :
    Download file extension virtualbox.org (VirtualBox 6.1.14 Oracle VM VirtualBox Extension Pack) sesuaikan dengan versi anda
    selanjutnya yaitu :
    
      1. klik "file" (di kiri pojok atas)
      2. pilih "preference"
      3. pilih "extension"
      4. pilih icon kotak hijau dengan tanda plus (+) dan akan di arahkan ke file manager
      5. pilih file yang sudah di download tadi
      6. masukan password root
      7. selesai siap digunakan
